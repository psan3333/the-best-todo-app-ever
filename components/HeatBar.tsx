import React from "react";
import { Text, View } from "react-native";

const HeatBar = ({ date, count }: { date: Date; count: number }) => {
    return (
        <View>
            <Text>HeatBar</Text>
        </View>
    );
};

export default HeatBar;
